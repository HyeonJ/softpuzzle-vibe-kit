# Repository Instructions

## 프로젝트 목적

이 저장소는 Codex로 소프트웨어 프로젝트를 기획부터 배포까지 수행하기 위한 플러그인인 Softpuzzle Vibe Kit을 담고 있습니다. (Claude Code 환경에서 사용할 때는 `CLAUDE.md`를 참조하세요.)

주 사용자는 한국 실무자입니다. 따라서 사용자에게 노출되는 설명, 기본 프롬프트, 템플릿 본문, 산출물 작성 가이드는 한글로 작성합니다. 단, 플러그인 이름, 스킬 이름, 디렉터리명, 파일명, JSON key, 스크립트 식별자는 영어를 유지합니다.

## 구조 규칙

- 마켓플레이스 배포 단위는 하나의 플러그인으로 유지합니다.
- 업무 방법론과 절차는 `skills/` 아래에 모듈형으로 둡니다.
- 반복 가능한 자동화는 `scripts/` 또는 `tools/`에 둡니다.
- 재사용 가능한 산출물 양식은 `templates/`에 둡니다.
- 공통 품질 기준과 인수인계 규칙은 `standards/`에 둡니다.
- 역할 정의와 위임 기준은 `agents/`에 둡니다.
- 고객사별 실제 프로젝트 데이터는 이 플러그인에 넣지 않습니다.

## 언어와 이름 규칙

- 플러그인, 스킬, 스크립트, 디렉터리 이름은 영어 lower-case hyphen-case를 사용합니다.
- 한국 사용자가 읽는 설명과 프롬프트는 한글로 작성합니다.
- 한 번 배포한 `name` 값은 가볍게 바꾸지 않습니다.
- 추상적인 제품명보다 실제 업무를 설명하는 명확한 이름을 우선합니다.

## 플러그인 Manifest 규칙

- `.codex-plugin/plugin.json`은 항상 유효한 JSON이어야 합니다.
- 플러그인 `name`은 `softpuzzle-vibe-kit`을 사용합니다.
- 표시 이름은 `Softpuzzle Vibe Kit`을 사용합니다.
- 경로는 플러그인 루트 기준 상대 경로를 사용합니다.
- 실제 값이 확정되지 않은 URL, 이메일, 약관 링크는 임시값임이 드러나게 둡니다.
- 플러그인 스키마가 바뀌지 않는 한 필수 필드를 제거하지 않습니다.

## 스킬 작성 규칙

- 모든 스킬 디렉터리는 `SKILL.md`를 가져야 합니다.
- 모든 `SKILL.md`는 `name`과 `description` frontmatter를 가져야 합니다.
- 스킬의 `name`은 디렉터리명과 일치해야 합니다.
- 하나의 스킬은 하나의 프로젝트 단계나 명확히 제한된 역량만 담당합니다.
- 각 스킬에는 사용 시점, 기대 산출물, 작업 절차를 포함합니다.
- 다음 단계가 재사용해야 하는 양식은 대화에만 남기지 말고 `templates/`에 둡니다.

## 템플릿 규칙

- 템플릿은 여러 프로젝트와 고객사에서 재사용 가능해야 합니다.
- 유지보수할 수 있을 만큼 간결하게 작성합니다.
- 요구사항, 화면, API, 테스트 케이스처럼 다음 단계가 참조하는 항목은 안정적인 ID를 사용합니다.
- 결정사항, 가정, 오픈 이슈, 리스크를 구분합니다.

## 도구 규칙

- 도구는 결정적이고 반복 가능해야 합니다.
- 생성, 검증, 변환, 품질 게이트에 도구를 사용합니다.
- 판단과 방법론은 스크립트가 아니라 스킬에 둡니다.
- 로컬 문서 흐름이 안정되기 전에는 외부 연동을 추가하지 않습니다.

## 편집 규칙

- 요청받은 단계나 기능에 맞춰 변경 범위를 좁게 유지합니다.
- 배포된 식별자는 가볍게 이름을 바꾸지 않습니다.
- 한 영역을 수정하면서 관련 없는 템플릿이나 스킬을 대규모로 다시 쓰지 않습니다.
- 한글 본문은 UTF-8로 보존합니다.
- 코드와 파일명은 특별한 이유가 없으면 ASCII를 사용합니다.

## 검증

플러그인 메타데이터를 수정한 뒤에는:

- `.codex-plugin/plugin.json`을 JSON으로 파싱합니다.
- 메타데이터 확정 작업이라면 `[TODO:`가 남아 있는지 확인합니다.

스킬을 추가하거나 수정한 뒤에는:

- 해당 스킬에 `SKILL.md`가 있는지 확인합니다.
- frontmatter에 `name`과 `description`이 있는지 확인합니다.
- 스킬 이름과 디렉터리명이 일치하는지 확인합니다.

스크립트를 추가한 뒤에는:

- 짧은 사용법을 문서화합니다.
- 가능하면 안전한 샘플 경로에서 실행해 봅니다.

